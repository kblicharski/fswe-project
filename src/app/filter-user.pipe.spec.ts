import { FilterPipePipe } from './filter-user.pipe';

describe('FilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
